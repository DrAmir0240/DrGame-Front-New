# ============================================================
# Production environment — optimized standalone image
# Usage: docker compose -f docker-compose.prod.yml up --build -d
# ============================================================

services:
  frontend:
    image: ghcr.io/dramir0240/drgame-front-new:latest
    restart: unless-stopped
    env_file: .env
    networks:
      - webnet

networks:
  webnet:
    external: true
